services:
  - type: web
    name: nestjs-backend
    env: node
    plan: free   
    buildCommand: npm install && npm build
    startCommand: npm start:prod
    autoDeploy: true

    envVars:
      - key: NODE_ENV
        value: production

      - key: PORT
        value: 3000
